# TrustSentinel Backend Dependencies
# FastAPI + ASGI Server
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Redis with Bloom Filter support
redis==5.0.1

# Async HTTP client for fetching URLHaus data
httpx==0.26.0

# Environment configuration
python-dotenv==1.0.1

# URL parsing and validation
validators==0.22.0

# Image processing & Deepfake Detection
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# PyTorch for ResNext+LSTM model (CPU version for compatibility)
# For GPU: install torch with CUDA separately
torch>=2.0.0
torchvision>=0.15.0
tqdm>=4.65.0

# File upload handling
python-multipart>=0.0.6
